function model = fitModel(characterImages, labels)
    % train the model with characterImages and their labels
    
    % 1. Extract the character images from the captchaImage
    % 2. Split CAPTCHA labels into corresponding character label, eg 'a1df'
    %       => 'a' '1' 'd' 'f'
    % 3. Train CNN or use OCR using the generated character images and
    %       corresponding labels
end